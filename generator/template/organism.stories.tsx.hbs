import { ComponentMeta, Story } from '@storybook/react';
import { {{pascalCase name}}, {{pascalCase name}}Props, storyObj } from './';

export default {
  title: '{{atomic}}/{{dirname}}/{{pascalCase name}}',
  component: {{pascalCase name}},
} as ComponentMeta<typeof {{pascalCase name}}>;

const Template: Story<{{pascalCase name}}Props> = (args) => <{{pascalCase name}} {...args} />;

export const Default = Template.bind({});
Default.args = storyObj.default;
