import { render, RenderResult } from '@testing-library/react';
import { {{pascalCase name}}, {{pascalCase name}}Props, storyObj, baseId } from './';

let res: RenderResult;
let props: {{pascalCase name}}Props;

describe('component/{{atomic}}/{{pascalCase name}}', () => {
  describe('default', () => {
    beforeEach(() => {
      props = storyObj.default;
      res = render(<{{pascalCase name}} {...props} />);
    });

    it.todo('component/{{atomic}}/{{pascalCase name}} のテストケースを書くこと');
  });
});