import { render } from '@testing-library/react';
import { {{pascalCase name}}, baseId } from './{{pascalCase name}}';
import { {{camelCase name}}Props } from './{{pascalCase name}}.props';
import { {{pascalCase name}}Props } from './{{pascalCase name}}.type';

let props: {{pascalCase name}}Props;

describe('component/{{atomic}}/{{pascalCase name}}', () => {
  describe('default', () => {
    beforeEach(() => {
      props = {{camelCase name}}Props.default;
    });

    it('component/{{atomic}}/{{pascalCase name}} のテストケースを書くこと', () => {
      const view = render(<{{pascalCase name}} {...props} />);
      expect(true).toBe(true);
    });
  });
});